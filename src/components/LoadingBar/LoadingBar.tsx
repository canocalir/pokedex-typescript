import { LoadingBarContainer, Spinner } from "./LoadingBar.styled"

const LoadingBar = () => {
  return (
    <LoadingBarContainer>
        <Spinner/>
    </LoadingBarContainer>
  )
}

export default LoadingBar