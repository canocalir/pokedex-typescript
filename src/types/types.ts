type AllResult = {
  name: string;
  url: string;
};

type HeldItemName = {
  name: string;
};

type PokeCard = {
  isDarkMode: boolean;
};

type ItemsColor = {
  itemsColor: string;
};

export type { AllResult, HeldItemName, PokeCard, ItemsColor };
