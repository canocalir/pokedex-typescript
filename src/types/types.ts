type AllResult = {
  name: string;
  url: string;
};

type HeldItemName = {
  name: string;
};

type ItemsColor = {
  itemsColor: string;
};

type Theme = {
  isDarkMode?: boolean;
};

export type { 
  AllResult, 
  HeldItemName,
  Theme,
  ItemsColor };
